% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doIntermediatefiltering.R,
%   R/doStrictfiltering.R
\name{write_with_header_base}
\alias{write_with_header_base}
\title{write out file with comment lines}
\usage{
write_with_header_base(
  df,
  path,
  sep = NULL,
  row.names = FALSE,
  na = "",
  flatten_lists = TRUE,
  header_override = NULL,
  add_blank_after_header = FALSE,
  quote = TRUE,
  ...
)

write_with_header_base(
  df,
  path,
  sep = NULL,
  row.names = FALSE,
  na = "",
  flatten_lists = TRUE,
  header_override = NULL,
  add_blank_after_header = FALSE,
  quote = TRUE,
  ...
)
}
\arguments{
\item{df}{Data frame to write. If it has \code{attr(df, "raw_header_lines")}, those lines
are written before the header row unless \code{header_override} is supplied.}

\item{path}{Character. Output file path.}

\item{sep}{Field separator. If \code{NULL} (default), inferred from \code{path}}

\item{row.names}{Logical; passed to \code{utils::write.table()}. Default \code{FALSE}.}

\item{na}{String used for missing values. Default \code{""}.}

\item{flatten_lists}{Logical; collapse list-columns to \verb{;}-separated strings. Default \code{TRUE}.}

\item{header_override}{Optional character vector of header lines to write instead of
\code{attr(df, "raw_header_lines")}.}

\item{add_blank_after_header}{Logical; write a blank line after the header lines. Default \code{FALSE}.}

\item{quote}{Logical; quote character columns. Passed to \code{utils::write.table()}. Default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{utils::write.table()}.}
}
\value{
Invisibly returns \code{normalizePath(path)}. Side effect: writes \code{path}.

Invisibly returns \code{normalizePath(path)}. Side effect: writes \code{path}.
}
\description{
This function is used to write out csv files in the Brioche pipelines to preserve their appended headers of filtering information

This function is used to write out csv files in the Brioche pipelines to preserve their appended headers of filtering information
}
\author{
James O'Dwyer
}
\keyword{comment}
\keyword{csv,}
\keyword{genome,}
\keyword{lines,}
\keyword{write}
