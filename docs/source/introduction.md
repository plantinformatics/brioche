# Introduction

## Overview

Brioche is a pipeline for mapping markers to a reference genome and supporting downstream workflows such as re-mapping between references and in silico genotyping.

## Key uses

1. {doc}`Usecase1: Remapping data across reference genomes <usecases/usecase1_remapping>`
2. {doc}`Usecase2: In silico genotyping of reference genomes <usecases/usecase2_insilico_genotyping>`
3. {doc}`Usecase3: Testing redundancy/accuracy in marker datasets <usecases/usecase3_redundancy_accuracy>`
4. {doc}`Usecase4: Merge datasets <usecases/usecase4_merge_datasets>`