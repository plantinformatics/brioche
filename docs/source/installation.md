# Installation

## Downloading

Describe cloning the repository and any required system dependencies.

## File layout

Explain key folders (e.g. `bin/`, `configs/`, `params.config`, etc.) and where outputs go.

## Conda environments

Explain how Brioche uses a Conda environment file, where Nextflow caches envs, and how this interacts with Slurm/partitions.